{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceRoot}/cmd/downloader/main.go",
            "args": [
                // "--domain=microup.ru",
                "--domain=dbeaver.io",
                "--timestamp=202404",
                "--workers=2",
            ],
            "cwd": "${workspaceFolder}/build"
        }
    ]
}
